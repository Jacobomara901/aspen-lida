Aspen LiDA

Uses expo.io to build https://expo.io/

To setup for development

1) Install Node.js - see: https://nodejs.org/en/download/
2) Navigate to the code directory 
3) Run yarn from the code directory to retrieve dependencies
4) Copy templates in app-config-templates to app-configs and substitute your information

Run `node scripts/start.js` to run LiDA locally. To debug, you may need to first have a developer build within expo.

Run `node scripts/update.js` to create and update builds for LiDA using expo, or `node scripts/build-local.js` to build on this machine instead.

Optional
- Install Android Studio - https://developer.android.com/studio
